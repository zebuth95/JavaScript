const element = document.querySelector('.nombre');
const name = 'Leticia Fernández Sánchez';

const content =`El nombre de mi compañera es ${name} , y está compuesto por ${name.length} caracteres`;

element.innerHTML = content;-